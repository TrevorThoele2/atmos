#pragma once

#include "BattleActionManager.h"

namespace Atmos
{
    /*
    namespace Battle
    {
        class ActionPlacement : public ActionManager
        {
        private:
            std::unordered_set<GridPosition> placementSet;
            std::vector<CharacterEntity> charactersToPlace;

            void StartImpl() override;
            void StopImpl() override;
            bool CanStartImpl() const override;
            void OnDeselectEntityImpl() override;

            void OnMouseKeyPressedImpl(const Input::MouseKey &arg) override;

            void PlaceAt(const GridPosition &position);
        public:
            void AddPosition(const GridPosition &add);
        };
    }
    */
}